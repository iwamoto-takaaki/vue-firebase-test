import Vue from 'vue';
import Vuex from 'vuex';
import * as message from './modules/Message';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    message,
  },
});
